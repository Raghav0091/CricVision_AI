from detection.detection_schema import DetectionCandidate


def remove_static_candidates(candidates: list[DetectionCandidate]) -> list[DetectionCandidate]:
    """Interface placeholder; filtering is intentionally deferred to Milestone 6."""
    return list(candidates)
