# cricket_vision shared package

Future framework-independent CV contracts and measured geometry belong here. The package is intentionally small while the active implementation remains in `Backends/`.

Current contents are limited to useful calibration geometry, detection contracts/adapters, and report dataclasses. Tracking, trajectory, and replay stay in the active backend until a real migration replaces them; placeholder duplicates are not kept here.
